import SwiftUI

public struct LunaTheme {
    static let primary = Color.purple
    static let secondary = Color.indigo
    static let accent = Color(red: 0.8, green: 0.4, blue: 1.0)

    static let background = Color(red: 0.05, green: 0.05, blue: 0.12)
    static let surface = Color(red: 0.1, green: 0.1, blue: 0.18)
    static let surfaceElevated = Color(red: 0.15, green: 0.15, blue: 0.22)

    static let textPrimary = Color.white
    static let textSecondary = Color.white.opacity(0.7)
    static let textTertiary = Color.white.opacity(0.5)
}
